spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: INFO
    com.govos: INFO

management:
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
